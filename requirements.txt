lz4>=4.0
requests>=2.28

# gt3solver (login captcha) only — optional, heavy. See gt3solver/README.md.
#   offline Geetest w path:  pycryptodome, opencv-python, numpy
#   browser-harvest path (Geetest + 网易易盾): patchright (+ a Chrome install), opencv-python, numpy
#     (requests, already required above, is also used to fetch the 网易 slide images)
# pycryptodome>=3.20
# opencv-python>=4.9
# numpy>=1.26
# patchright>=1.60
